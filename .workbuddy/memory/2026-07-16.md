# 2026-07-16

## 会话标题自动生成

- 已有 `SessionTitleChangedEvent`（Models/Event.cs）和 `ISession.Title` / `CreateSessionTitle`
- `HandleSendMessage`：首条消息后异步调用 `CreateSessionTitle`，设置 `session.Title`，通过 SessionBridge 通知前端
- `SessionBridge`：订阅 `SessionTitleChangedEvent`，转发 `session_title_changed`
- `WsProtocol`：+`SessionTitleChanged` 工厂，`SessionSummary` +`Title`
- `PushSessionList`：`Title` 为空时默认 `"新建对话"`
- 前端：`Sidebar.tsx` 改用 `s.title || '新建对话'`，`sessionStore` +`updateSessionTitle`
- `_titleGenerating` HashSet 防重复触发

## 手动重命名会话标题

- 双击 Sidebar 标题 → 进入编辑模式 → Enter/失焦保存 → rename_session + title
- `WsMessageHandler.HandleRenameSession`：更新 `session.Title`，广播 `session_title_changed`
- `WsProtocol.ClientMessage` +`title`，前端 `ClientMessage` 同步
